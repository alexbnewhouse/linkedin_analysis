"""Career/employment value cleaning: build & evaluate canonicalization approaches.

Targets the parsed `experience` + `positions` tables and the three career fields
we want to clean: organization (`company`), position `title`, and `description`.
Structured to mirror `edu_clean/` so the two efforts share conventions.
"""
